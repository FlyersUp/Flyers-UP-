/**
 * POST /api/bookings/[bookingId]/progress/final-request
 * Same contract as POST /api/bookings/[bookingId]/complete — final job photos after all milestones are confirmed.
 */
export { POST } from '../../complete/route';

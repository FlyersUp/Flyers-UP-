# Supabase Configuration
# Copy this file to .env.local and fill in your values from the Supabase dashboard

# Public keys (safe for browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional (recommended): server-only upstream Supabase URL (prevents proxy loops)
# If set, server-side code uses this while browser requests can be proxied via /api/supabase.
SUPABASE_URL=https://your-project-url.supabase.co

# Server-side only (NEVER expose to client)
# Used for admin operations like creating profiles after signup
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Admin dashboard access: comma-separated emails allowed to access /admin
# Canonical admin: Hello.flyersup@gmail.com
ADMIN_EMAILS=Hello.flyersup@gmail.com

# Stripe (optional)
# If not set, Stripe functionality will be disabled.
STRIPE_SECRET_KEY=sk_test_your_key_here

# Stripe Webhook (required only if you use /api/stripe/webhook)
STRIPE_WEBHOOK_SECRET=whsec_your_secret_here

# Client-side Stripe key (only needed if you add Stripe.js on the client)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_key_here





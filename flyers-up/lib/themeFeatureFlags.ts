/**
 * When false, the app stays in light mode: no `.dark` on `<html>`, appearance toggles hidden.
 * Tailwind `dark:` classes and `globals.css` `.dark { … }` blocks remain for a future re-enable.
 * Set to `true` to ship dark mode again.
 */
export const DARK_MODE_END_USER_ENABLED = false;
